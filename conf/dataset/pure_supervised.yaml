dataset: pure_supervised
fps: 30
frame_width: 64
frame_height: 64
# Override training defaults (requires dataset merged after training in config_merge).
optimization_step: supervised
validation_step: supervised
losses: supervised
validation_loss: supervised
